# 交接说明 · 小米手环直播间销量分析

> **这份文档是给「接手的人 + 他的 AI 工具」看的，不依赖任何本机配置。**
> 拿到项目后，**先让 AI 完整读一遍本文档**，之后每天只做两件事：录入订单、录入业绩。
>
> 更细的知识库在 `WORKFLOW.md` 和 `docs/`，但**日常跑数据只看本文档就够了**。

---

## 📋 零、把这段直接发给你的 AI 工具（复制即用）

```
你现在接手一个数据分析项目「小米手环直播间销量分析」，工作目录就是这个仓库。

请先完整读一遍根目录的《交接说明.md》，再读 WORKFLOW.md 顶部的「⚡ 每日执行清单」。
读完后你要能做到：我给你一个 x.xx日订单.xlsx 或 x.xx日业绩.xlsx，
你自己判断类型、解析、入库、自校验、commit 到 git，最后用简表回报结果。

规则：这两个流程都是【已授权的全自动流程】，跑完为止，中途不要逐步问我「要不要继续」。
只有《交接说明.md》第八节列的 6 种情况才停下来问我。

现在读完文档后，先用一句话告诉我：这个项目是干什么的、两条流程分别写哪个文件。
```

> 把上面这段整块发给 AI，它就会进入状态。以后每天你只需要把 Excel 拖给它。

---

## 一、这个项目是什么

小米手环（及手表/耳机/AI眼镜等）**直播间销售数据的日常入库与可视化平台**。

每天从直播后台导出两个 Excel，分别入库到两条**互相独立**的链路：

| 文件 | 内容 | 数据落到哪 |
|------|------|-----------|
| `x.xx日订单.xlsx` | 每笔订单明细 | `sales_analysis/history.json` 等（**销售分析链路**） |
| `x.xx日业绩.xlsx` | 每个主播每班次的 GSV | `主播业绩/业绩demo.html`（**主播业绩链路**） |

最终产出：
- `index.html` / `sales_analysis/index.html` —— 网页看板（各直播间销量、小时明细、商品结构）
- `主播业绩/业绩demo.html` —— 主播业绩面板（日第一、月度出勤、退款比率）
- `月度总结/*.html` —— 月度汇总页
- 桌面《小米手环11首销月目标进度表.xlsx》—— 首销月进度（**仅 2026-09-07~10-07 有效**）

---

## 二、⚠️ 三条铁律（先记这个，违反了会毁数据）

1. **两条流程不许互相污染**
   订单流程**只碰 `sales_analysis/`**；业绩流程**只碰 `主播业绩/业绩demo.html`**。
   （例外：首销月总结链路会读 `history.json`，但只读不写。）

2. **禁止 `git add -A`**
   工作区常年躺着历史遗留文件（`直播间销量汇总工具.html`、`_vw.py`、`gen_watch_compare.py`）。
   一律 **`git add <本次真正改动的文件>`**，并在回报里说明还有哪些没提交。

3. **脚本生成的文件，改 HTML 无效**
   月度总结页、耳机专报等是 `.py` 生成的，只改 HTML 会在下次重跑时被覆盖。
   **要改就改生成器脚本。**

---

## 三、环境准备（只做一次）

### 3.1 拉代码

```bash
git clone git@github.com:dongjie-study/xiaomi-band-dashboard.git
cd xiaomi-band-dashboard
```

> 改过仓库地址的话，把上面的 URL 换成你自己的。

### 3.2 装依赖

需要 **Python 3.10+**（本项目在 3.12 上跑），装 5 个第三方库：

```bash
pip install pandas numpy openpyxl matplotlib pillow
```

> 项目**没有** `requirements.txt`，依赖就是上面这 5 个。
> `openpyxl` 读写 Excel、`pandas` 解析、`matplotlib` 画 PNG 图表、`Pillow` 处理图标。

### 3.3 改掉文档里写死的路径

原作者的 `WORKFLOW.md` 里有几处写死了 `C:/Users/Administrator/Desktop/小米手环直播间销量分析`。
**你的目录不一样，直接用会报错。** 建议统一用相对路径，或先记录你自己的项目绝对路径：

```bash
# 先拿到你自己的项目绝对路径，后面所有命令都用它
pwd
```

本文档下面所有命令都以**项目根目录为当前目录**书写，请先 `cd` 到项目根。

### 3.4 ⚠️ Windows 必读：加 `PYTHONUTF8=1`，否则中文全是乱码 / 脚本会崩

Windows 控制台默认 GBK 编码，而这个项目**到处是中文**。不加这个环境变量会遇到两种症状：

| 症状 | 例子 |
|------|------|
| 中文输出变成乱码 | 列名打印成 `['ѡ����Ʒ', '�����ύʱ��', ...]` |
| 脚本直接崩溃 | `tools/gen_room_docs.py --check` 抛 `UnicodeEncodeError: 'gbk' codec can't encode character '✓'` |

**解决办法**（三种任选，推荐第一种）：

```bash
# ① 每条命令前加前缀（本文档后续命令都默认你已加）
PYTHONUTF8=1 python band11_review.py status

# ② 一次性设好，当前终端会话有效
export PYTHONUTF8=1
export PYTHONIOENCODING=utf-8

# ③ 永久生效（写进 ~/.bashrc）
echo 'export PYTHONUTF8=1' >> ~/.bashrc
```

> `run_all.py` 内部已经自己设了 `PYTHONIOENCODING` / `PYTHONUTF8`，所以它不用管；
> 但其他脚本和 `python -c` 一行命令**都需要**。

### 3.5 克隆后会缺的东西（正常现象，不是坏了）

`.gitignore` 排除了一些**可再生的**文件，新克隆的仓库里会没有：

| 缺什么 | 为什么 | 怎么办 |
|--------|--------|--------|
| `*.xlsx`（订单/业绩源文件） | 数据敏感，不入库 | 自己从后台导 |
| `*.png`（`dashboard.png` 等图表） | 体积大 | 跑一次订单流程就重新生成 |
| `output/` 整个目录 | 构建产物 | 跑 `run_all.py` 自动重建 |

> **数据主库 `history.json` / `daily_summary.json` 是进了 git 的**，克隆下来就有全部历史，
> 不用从零开始。这是这个项目最值钱的部分，**改动它们前务必先备份**。

---

## 四、🅰️ 每日订单流程（最常用，7 步）

### 第 0 步：先看进度，避免重复入库

```bash
python band11_review.py status
```

输出示例：

```
history 最新 : 2026-09-18     ← 订单已入库到哪天
总结最新     : 2026-09-18     ← 每日总结写到哪天
待补总结     : 无 ✔          ← 这里出现日期 = 那天漏写总结了
```

> ⚠️ 如果 `history 最新` **已经等于**你手上这份文件的日期 → **已经入过库了**，
> 停下来问用户是覆盖还是忽略，**不要直接重跑**。

### 第 1 步：认文件（必做，别只看文件名）

用 Python 打开看列名：

```bash
PYTHONUTF8=1 python -c "import pandas as pd,sys; df=pd.read_excel(sys.argv[1],nrows=3); print(list(df.columns))" "<订单文件路径.xlsx>"
```

应该打出类似 `['选购商品', '订单提交时间', '订单状态', '订单应付金额', '达人昵称']`。
若打出乱码，是你漏了 `PYTHONUTF8=1`，见 3.4。

**订单文件**的列名特征（同时满足）：

- 含 `选购商品`
- 含 `订单状态`
- 含 `订单应付金额`
- 有一个**时间列**
- 有一个**直播间列**

> 📌 **列名会变，看含义不看字面**：时间列见过 `订单提交时间` / `支付完成时间`（9.11 起改后者），
> 直播间列见过 `直播间名称` / `达人昵称`。解析器走关键词匹配，不受影响。

### 第 2 步：入库

```bash
python run_all.py sales "<订单文件路径.xlsx>"
```

脚本会自动：解析 → 更新 `history.json` / `band10pro_history.json` / `band11_history.json`
→ 重建 `stats_data.js` → 写 `hourly/<日期>.json` → 重新生成图表 PNG。

### 第 3 步：自校验（**必做，不过就停下查**）

```bash
# ① 日期没坏（NaT 会让进度表静默少一天）
python -c "import json;H=json.load(open('sales_analysis/history.json',encoding='utf-8'));print(H[-1]['date'],H[-1]['total_orders'])"

# ② 各直播间条数之和 == 总条数
python -c "import json;H=json.load(open('sales_analysis/history.json',encoding='utf-8'));r=H[-1];print(sum((v or {}).get('orders',0) for v in r['rooms'].values()),'vs total',r['total_orders'])"

# ③ 总结库自检
python band11_review.py validate
```

- **①** 打出 `NaT` → **停下**，让用户重新导出（导出时排除「待支付」），按第十节清理。
- **②** 两个数应**相等**。不等就逐间比对源文件；差额通常来自被忽略的「小米手环手表直播间」（约 5 单/天）。
- **③** 应输出「校验通过 ✔」。

### 第 4 步：看当天事实（仅首销月需要）

```bash
python band11_review.py context 2026-09-18
```

会打印当天各渠道台数、环比、累计进度等，作为写总结的素材。

> ⚠️ **日期只认 `YYYY-MM-DD` 全格式**。写成 `9.18` 会直接抛
> `ValueError: time data '9.18' does not match format '%Y-%m-%d'`。
> `status` 命令可省略日期（默认取最新一天）。

### 第 5 步：写每日总结稿（仅首销月需要）

新建 `sales_analysis/daily_review_input/YYYY-MM-DD.json`，**只有 4 个字段，每段 ≤ 90 字**：

```json
{
  "rating": "一般",
  "good": "手表间 54 台环比 +170%，是当日唯一上涨的渠道；商品卡 888 台反超数码升到第二",
  "bad": "环比 -16.7% 结束三连涨，手环间少 405 台，占全天 -557 台降幅的 73%",
  "watch": "手环间能否回到 1,200 台以上，看工作日流量回归，预计 2-3 天见分晓"
}
```

| 字段 | 要求 |
|------|------|
| `rating` | `好` / `一般` / `差` 三档，人工写的会覆盖脚本自动评级 |
| `good` | **必须点出好在哪**：渠道 + 数字 + 对比。**不要写「整体平稳」这类中性话** |
| `bad` | 同上，差在哪 |
| `watch` | **必须带时间预期**（如「预计 2-3 天见分晓」），否则接班的人不知道盯多久 |
| 口径 | **只看当天整体销售（订单口径），不要出现主播名/班次/主播 GSV** —— 那在业绩面板里 |

### 第 6 步：落库 + 刷新进度表（仅首销月需要）

```bash
python band11_review.py add YYYY-MM-DD      # 把稿子追加进 daily_summary.json
python generate_band11_target.py             # 重新生成桌面进度表 xlsx
```

> ⚠️ 跑 `generate_band11_target.py` 前**先关掉 Excel 里打开的进度表**，
> 否则会抛 `PermissionError`（脚本已转成中文提示）。
> ⚠️ **不要在桌面那张 xlsx 里手工编辑**——脚本每次都从零重建整个工作簿，手改会在下次重跑时**无声丢失**。

### 第 7 步：提交推送

见第七节「📦 提交推送」，commit message 用 `feat: x.xx日订单数据更新`。

> ⏳ **首销月附加条款**：第 4~6 步**只有 2026-09-07 ~ 10-07 需要做**。
> 10-07 之后跳过 4~6，只做 1~3 + 7。

---

## 五、🅱️ 每日业绩流程（4 步）

### 第 1 步：认文件

**业绩文件**的列结构固定：

| 列 | 内容 |
|----|------|
| A 列 | 主播名字 |
| B 列 | 主播名字（与 A 对应） |
| C 列 | 场次：`A1/A2`、`B1/B2`、`C1/C2`、`D1/D2`、`E` |
| E 列 | **每小时明细**（不是总业绩！） |
| F 列 | **该主播当天总 GSV** ← **取这一列** |
| G 列 | 日期 |

直播间在表里**纵向堆叠**：第 1 行 = 小米数码旗舰店，第 27 行 = 小米官方手环直播间，依次往下。

### 第 2 步：解析并插入 `DAILY_RECORDS`

目标是 `主播业绩/业绩demo.html` 第 ~1007 行的 `const DAILY_RECORDS = { ... }`。

**格式**（每天一个日期键，值是该天的记录数组）：

```javascript
'2026-09-17': [
    { roomId: 'room_xiaomi_digital', shift: 'A', anchor: '张蓬元', sales: 12931.99 },
    { roomId: 'room_xiaomi_digital', shift: 'B', anchor: '高珊珊', sales: 9169.49 },
    // ... 每个直播间 × 每个班次 一条
],
```

**操作步骤**：

1. 用 pandas 读 Excel，按直播间名切块，块内**每遇到一次非空「达人」名就是一条新记录**。
2. 取 **F 列**作为 `sales`。
3. 转成 `{ roomId, shift, anchor, sales }`。
4. **插在上一天块之后**（日期升序，最新的在最下面）。

**直播间 → roomId 映射**（Excel 里的名字必须换成下面的 id）：

| Excel 直播间名 | roomId |
|---------------|--------|
| 小米数码旗舰店 | `room_xiaomi_digital` |
| 小米官方手环直播间 | `room_xiaomi_band` |
| 小米官方手表 | `room_xiaomi_watch` |
| 小米官旗手表直播间 | `room_xiaomi_watch_flagship` |
| 小米官方耳机直播间 | `room_xiaomi_earphone` |
| 小米AI眼镜直播间 | `room_xiaomi_glasses` |
| 小米智能设备旗舰店直播间 | `room_xiaomi_smart_device` |
| 小米手环官旗直播间 | `room_xiaomi_band_flagship` |
| 手环预约期业绩（伪直播间，非真实渠道） | `room_xiaomi_band_preorder` |

**场次映射**：`A1`/`A2` → `'A'`，`B1`/`B2` → `'B'`，`C1`/`C2` → `'C'`，`D1`/`D2` → `'D'`，`E` → `'E'`（E 不分上下半场）。

### 第 3 步：自校验（**必做**）

1. **逐条比对**：重新解析 Excel，按 `roomId + shift + anchor + sales` 排序，
   与页面里 `DAILY_RECORDS['<当天>']` 解析出来的数组排序后比对，**必须完全一致（含条数）**。
2. **语法校验**：把 `DAILY_RECORDS` 整块抽出来交给 node 解析一遍，确认原有日期块没被破坏、最后一天是当天：
   ```bash
   node -e "const s=require('fs').readFileSync('主播业绩/业绩demo.html','utf8');const m=s.match(/const DAILY_RECORDS = (\{[\s\S]*?\n            \};)/);const o=eval('('+m[1].replace(/;$/,'')+')');const ks=Object.keys(o).sort();console.log('块数',ks.length,'| 最新',ks[ks.length-1],'| 条数',o[ks[ks.length-1]].length)"
   ```
3. **主播姓名归一**：
   - Excel 里的**前后空格**（如 `' 朱泓颖'`）必须 strip。
   - 和页面既有写法不一致时（如「高姗姗」vs「高珊珊」），**按页面既有写法归一**，不要新建一个人。
   - 归一后确认该名字在页面里**不是孤例**。

### 第 4 步：提交推送

commit message 用 `feat: x.xx日业绩数据更新`。

### 第 4.5 步（可选）：刷新数据小管家的业绩数据

`assistant-server/`（DeepSeek 问答小管家）读的是 `主播业绩/anchor_records.json` 而不是 HTML。
业绩入库后**跑一次**：

```bash
PYTHONUTF8=1 python tools/extract_anchor_records.py   # 只读 业绩demo.html，抽取最新数据
git add 主播业绩/anchor_records.json                   # 随本次业绩 commit 一起提交
```

忘了跑也没事，小管家会优雅提示"业绩数据暂不可用"，不影响其他数据源。

**数据小管家线上入口**（已上线）：
- 正式地址 **https://data.xiaomi-exam.top** （口令 `xiaomi`；与考试系统 `xiaomi-exam.top` 同一 Cloudflare 账号，走子域名分流，国内可直连、无需备案）
- 工作台模块页 `数据小管家/index.html` → 读同一后端；独立聊天页就是这个地址的根路径
- 代码在 `assistant-server/`（Worker，数据读 GitHub raw，push 后自动最新）；域名绑定写在 `wrangler.toml` 的 `routes`
- 备用方案 `cloudbase-assistant/`（腾讯云 CloudBase 版，功能等价，暂未部署）

### 第 4.6 步（可选，仅腾讯云备用版小管家需要）：同步数据到腾讯云

`cloudbase-assistant/` 把数据**打进函数包**，所以数据更新后需要重新生成资产并部署一次：

```bash
python tools/build_cloudbase_assets.py                  # 生成 assets（读现有数据，只写 cloudbase-assistant/）
cd cloudbase-assistant && npx -p @cloudbase/cli tcb fn deploy chat
```

订单数据更新后可顺带跑一次；两三天跑一次也不影响使用（只是云端数据会滞后几天）。
Cloudflare 版（`assistant-server/`）读 GitHub raw，push 后自动最新，无需这一步。

### ⚠️ 业绩流程的坑

- **总 GSV 在 F 列，不是 E 列**。E 列是分小时明细。
- 偶尔有**总 GSV 掉到错列**的情况（如 9.12 耳机直播间李晓洋 B 班的 3208.6）——
  用**小时明细加总**验证后再入库，差额能解释就入库并在回报里写清依据。
- 行尾可能有多余空格（时间、姓名都可能带），全部 `strip`。
- **不要**运行 `run_all.py sales`（那是订单流程，会改 `sales_analysis/`）。
- **不要**运行 `archive/` 里的任何脚本。

---

## 六、🅲 两个文件同时给到

**两条流程各自独立跑**：

1. 先跑 🅰️ 的第 1~3 步（订单入库 + 自校验）
2. 再跑 🅱️ 的第 1~3 步（业绩解析 + 自校验）
3. 最后回到 🅰️ 的第 4~7 步（写总结 → 刷新进度表 → 提交）

可以合成**一个 commit**，也可以分两个 commit，都行。

---

## 七、📦 提交推送（两条流程通用）

```bash
git add <精确列出本次改动的文件>          # ⚠️ 绝不用 git add -A
git commit -m "feat: x.xx日订单数据更新"   # 业绩则用 "feat: x.xx日业绩数据更新"
git pull --rebase
git push
git log --oneline -1                      # 确认落地
```

**push 被拒时**（远端有别人的新提交）：

```bash
# 若报 "cannot pull with rebase: You have unstaged changes"
git stash push
git pull --rebase
git stash pop
```

**订单流程典型要 add 的文件**：

```
sales_analysis/history.json
sales_analysis/band11_history.json
sales_analysis/b10pro_history.json
sales_analysis/stats_data.js
sales_analysis/hourly/<日期>.json
sales_analysis/daily_summary.json          # 首销月写总结时
sales_analysis/daily_review_input/<日期>.json
```

**业绩流程典型要 add 的文件**：

```
主播业绩/业绩demo.html
```

> 桌面那张进度表 xlsx 被 `.gitignore` 排除（`*.xlsx`），**不进 git**，不用管。

---

## 八、🛑 只有这 6 种情况才停下来问用户

**除下表之外，默认全部自动做完，不要逐步确认。**

| 情况 | 怎么处理 |
|------|---------|
| 出现**从未见过的新直播间名** | **先问归属**，不要静默归到「良米」。见下方备注 |
| 文件既不像订单也不像业绩，或列名对不上 | 问用户这是什么文件 |
| 数值对不上（如小时明细加总 ≠ 总 GSV 且差额无法解释） | 说明差异后问用户以哪个为准 |
| **同一天已入库、且新数据与旧数据不一致** | 问用户是覆盖还是忽略 |
| 用户给的日期与文件名日期不符 | 问用户用哪个日期 |
| 主播姓名页面里查无此人，且无法判断是不是同一人写法 | 问用户确认 |

> 📌 **新直播间归属速查**：以根目录 `直播间分类.md` 为准（或 `docs/03-直播间与团队.md`）。
> 归属总表由 `team_config.py` **单向生成**，不要手改 markdown。
> 遇到表里没有的名字 → **问用户**。

> 📌 **能自动归一的不用问**：姓名同音/错别字，只要能由页面既有写法**唯一确定**
> （如「高姗姗」→「高珊珊」、「 朱泓颖」→「朱泓颖」），直接按既有写法归一，**在回报里说明即可**。

---

## 九、📣 跑完怎么回报

一条简表，**不要中途逐步确认**：

> 文件类型 → 录了哪个日期、几条 → 做了哪些归一/取舍（含依据）→ 校验结果 → commit hash

再加一句**结论**：当天卖得好不好、明天盯什么。

---

## 十、🚑 出问题怎么办

### 10.1 常见故障速查

| 症状 | 原因 | 处理 |
|------|------|------|
| `history.json` 最新条 `date` 是 `NaT` | 源文件首行是「待支付」订单（无支付完成时间），`daily_update.py:195` 取**第一行**的支付时间 | **让用户重新导出**（排除待支付），再按 10.2 清理。**不要自己猜日期** |
| 进度表莫名少一天，且不报错 | 同上——`load_daily()` 遇到 `NaT` 会 `strptime` 失败并**静默跳过** | 同上 |
| `generate_band11_target.py` 抛 `PermissionError` | Excel 正打开着那张表 | 关掉 Excel 再跑 |
| 同名重复入库，`history.json` 出现两条同日期 | 同日 upsert 只在日期完全一致时才覆盖；`NaT` 会让 upsert 失效 | 按 10.2 清理重复那条 |
| `daily_update.py` 报找不到列 | 列名变了（月份不同列名会变） | 先看实际列名，对照第四节第 1 步的特征判断 |
| 数值比源文件少几条 | 被忽略的「小米手环手表直播间」 | 正常，约 5 单/天，在 `team_config.IGNORED_ROOMS` 里 |
| 两个「销售额」对不上（如 455,523 vs 472,176） | **口径不同**：进度表取订单**实付**，业绩面板取主播 GSV（**未扣退款**） | 不是 bug，见 `docs/06` |
| 中文输出全是乱码，或 `UnicodeEncodeError: 'gbk' codec` | Windows 控制台 GBK 编码 | 命令前加 `PYTHONUTF8=1`，见 3.4 |
| `gen_room_docs.py --check` 报「已过期」但你没改过 `team_config.py` | ~~已知假警报~~ **2026-09-26 已修复**：`--check` 现已忽略日期戳，只比对内容 | 现在报「已过期」就是**真过期**，重跑 `gen_room_docs.py` 即可 |

### 10.2 修坏数据的标准动作（改 `history.json` 这类主库时）

`history.json` 是**唯一副本**，改它必须按下面 4 步，**不许直接手改**：

1. **先备份**
   ```bash
   cp sales_analysis/history.json "sales_analysis/history.json.bak-$(date +%Y%m%d-%H%M)"
   ```
   （`.gitignore` 已忽略 `*.bak-*`，不会污染仓库）

2. **带严格断言再落盘**：脚本里先 `assert` 总条数、目标记录的特征值（日期/条数）、前后邻居，
   **全过才写**。不要做「未点名的批量删除」。

3. **清掉坏运行的附带产物 —— 兄弟文件也要查**：
   坏运行不止写 `history.json`，还会往 `band11_history.json`、`b10pro_history.json`、`hourly/` 里写。
   ```bash
   # 一次查完所有数据文件的 NaT 残留
   python -c "import json,glob;[print(f,[r.get('date') for r in json.load(open(f,encoding='utf-8')) if r.get('date')=='NaT']) for f in glob.glob('sales_analysis/*_history.json')]"
   ls sales_analysis/hourly/ | grep -v '^2026'    # 有非日期文件名 = 垃圾
   ```

4. **修完重跑一遍入库 + 自校验**，确认与源文件对得上。

---

## 十一、不要做的事（红线清单）

| ❌ 不要 | 为什么 |
|--------|--------|
| `git add -A` | 会扫进别人的半成品文件 |
| 订单流程碰 `主播业绩/业绩demo.html` | 两条流程不许互相污染 |
| 业绩流程碰 `sales_analysis/` | 同上 |
| 跑 `archive/` 里的任何脚本 | 已结项，见 `archive/README.md` |
| 跑 `archive/update_daily_html.py` | 它会**整块改写** `业绩demo.html` 的 `DAILY_RECORDS` |
| 手改 `直播间分类.md` / `docs/03` / `直播间服务商汇总.md` | 由 `team_config.py` 单向生成，手改会丢 |
| 手改月度总结页 HTML | 由 `.py` 生成，重跑覆盖 |
| 手改桌面进度表 xlsx | 脚本每次从零重建，手改无声丢失。要改就改 `daily_summary.json` |
| 移动/改名 页面目录、`sales_analysis/` | 公开 URL + 硬编码相对路径依赖 |

---

## 十二、深入阅读（需要时再翻）

| 文档 | 内容 |
|------|------|
| `WORKFLOW.md` | **完整执行标准**。顶部「⚡ 每日执行清单」是日常版；往下是详细规则与例外 |
| `docs/00-索引.md` | 「我要做什么 → 去哪找」路由表 |
| `docs/01-数据流.md` | 三条链路、谁生成谁 |
| `docs/02-数据字典.md` | 各 JSON/JS 字段含义（**权威**） |
| `docs/03-直播间与团队.md` | 归属总表 + roomId |
| `docs/04-页面与模块.md` | 页面清单 + **改样式红线** |
| `docs/05-脚本清单.md` | 每个脚本是干嘛的、还能不能跑 |
| `docs/06-踩坑与故障.md` | 真踩过的坑 + 修法 |
| `CLAUDE.md` | 给 Claude Code 的项目说明（用别的 AI 工具可忽略，内容与本文档一致） |

### 换月了怎么办

文件名日期跨月时（如 `8.31日订单` → `9.1日订单`），**先准备新月份页面，再录当天数据**。
详细步骤见 `WORKFLOW.md`「四、月度切换」。

### 改直播间归属

改 `team_config.py`，然后**必须重跑生成器**：

```bash
PYTHONUTF8=1 python tools/gen_room_docs.py           # 重新生成 docs/03 + 直播间分类.md + 直播间服务商汇总.md
PYTHONUTF8=1 python tools/gen_room_docs.py --check   # 只检查，退出码 1 = 过期
```

> ✅ **`--check` 的日期戳假警报已于 2026-09-26 修复**：比对时会把文档头部的
> `最后生成/最后更新：<日期>` 行归一化再比较，**只有内容真的变了才报「已过期」**。
> （历史：修复前做字面全文比对，除生成器当天外每天都假报过期，
> 导致 `.github/workflows/docs-check.yml` 频繁误挂。）
> 现在 CI 再挂就是**真过期**：改过 `team_config.py` 忘了重跑生成器，重跑一次即可。

---

*本文档随项目走。发现流程有变，**请同步更新本文档**，否则下一个人会踩同样的坑。*
