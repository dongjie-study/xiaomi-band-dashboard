# 每日数据处理工作流

> **最后更新**: 2026-09-14
> **规则**: 每次执行前先读此文档。有新要求时及时更新此文档。
> **怎么看**: 日常**只读顶部「⚡ 每日执行清单」**就够了；出问题或命中例外时才往下翻对应章节。
> 📚 **知识库**: 非日常问题（字段含义 / 页面结构 / 脚本谁死谁活 / 踩过的坑）看 `docs/`，从 `docs/00-索引.md` 进。

> 📌 **直播间归属**：遇到直播间属于哪个团队（我司/良米/逐梦/凝云/斐纳/机械空间）的问题，以 `直播间分类.md` 为准，先查再执行。
> 🆕 **新直播间**：遇到 `TEAM_MAP` / `直播间分类.md` 里没出现过的新直播间名，**先找用户确认归属**再入库，不要静默兜底归「良米」。

---

## ⚡ 每日执行清单（每天只读这一节，照着做，不要跳步）

> 这是**日常唯一需要读的部分**，命令可直接复制。每一步的「为什么」和边界情况在下面各节，
> **只有在出问题、或命中「停下来问」时**才往下翻。

先统一工作目录，并花 5 秒看清当前进度到哪（**避免重复入库**）：

```bash
cd "C:/Users/Administrator/Desktop/小米手环直播间销量分析"
python band11_review.py status
```

```
history 最新 : 2026-09-13     ← 订单入库到哪天
总结最新     : 2026-09-13     ← 每日总结写到哪天
待补总结     : 无 ✔          ← 有日期在这里 = 那天漏写了总结
```

> 若 `history 最新` 已经是你手上这份文件的日期，说明**已经入过库了** —— 停下来问用户是覆盖还是忽略，
> 不要直接重跑（同日 upsert 只在日期完全一致时才覆盖）。

---

### 🅰️ 收到 `x.xx日订单.xlsx` → 7 步

| # | 做什么 | 命令 / 判据 |
|---|--------|------------|
| 1 | **认文件** | 列名含 `选购商品` + `订单状态` + `订单应付金额` + 一个时间列 + 一个直播间列。列名在不同月份会变（`订单提交时间`/`支付完成时间`、`直播间名称`/`达人昵称`），**看含义不看字面**，解析器走关键词匹配 |
| 2 | **入库** | `python run_all.py sales "C:\Users\Administrator\Desktop\x.xx日订单.xlsx"` |
| 3 | **自校验** | 跑下面「✅ 自校验三条铁律」，**有一条不过就停下查，不许带着疑问提交** |
| 4 | **看事实** | `python band11_review.py context x.xx` |
| 5 | **写总结稿** | 写 `sales_analysis/daily_review_input/YYYY-MM-DD.json`，字段 `rating`/`good`/`bad`/`watch`（模版见「📝 每日销售总结」） |
| 6 | **落库 + 刷新表** | `python band11_review.py add YYYY-MM-DD` → `python generate_band11_target.py` |
| 7 | **提交推送** | 跑下面「📦 提交清单」，然后按「📣 回报格式」给结论 |

> 第 6 步跑 `generate_band11_target.py` 前**先关掉 Excel 里打开的进度表**，否则会抛
> `PermissionError`（脚本已转成中文提示）。

> ⏳ **首销月附加**：仅 2026-09-07 ~ 10-07 有第 4~6 步（进度表 + 每日总结）。
> 10-07 之后跳过 4~6，只做 1~3 + 7。

---

### 🅱️ 收到 `x.xx日业绩.xlsx` → 4 步

| # | 做什么 | 命令 / 判据 |
|---|--------|------------|
| 1 | **认文件** | A列=主播名、C列=场次(A1/B2/E…)、**F列=总 GSV（不是 E 列）**、G列=日期 |
| 2 | **解析入库** | 逐行提取 → 转成 `{roomId, shift, anchor, sales}` → 插入 `主播业绩/业绩demo.html` 的 `DAILY_RECORDS`，**插在上一天块之后** |
| 3 | **自校验** | 逐条比对 + node 语法校验 + 姓名归一，见「六、入库自校验」 |
| 4 | **提交推送** | 跑下面「📦 提交清单」，commit message 用 `feat: x.xx日业绩数据更新` |

详细解析要点（坑）见「三、业绩文件」。

---

### 🅲 两个文件同时给到

两条流程**各自独立**跑：先跑 🅰️ 的 1~3 步，再跑 🅱️ 的 1~3 步，最后回到 🅰️ 的 4~7 步。

> ⛔ **不许互相污染**：订单流程**不许碰** `业绩demo.html`；业绩流程**不许碰** `sales_analysis/`。

---

### ✅ 自校验三条铁律（每次订单入库后、commit 之前）

```bash
# ① 日期没坏（NaT 会让进度表静默少一天）
python -c "import json;H=json.load(open('sales_analysis/history.json',encoding='utf-8'));print(H[-1]['date'],H[-1]['total_orders'])"
# ② 各直播间条数与源文件对得上（差额 = IGNORED_ROOMS 里的「小米手环手表直播间」，约 5 单/天）
python -c "import json;H=json.load(open('sales_analysis/history.json',encoding='utf-8'));r=H[-1];print(sum((v or {}).get('orders',0) for v in r['rooms'].values()),'vs total',r['total_orders'])"
# ③ 总结库自检
python band11_review.py validate
```

- ① 若打出 `NaT` → **让用户重新导出**（排除「待支付」），再按「修坏数据的标准动作」清理，**不要自己猜日期**。
- ② 两数应相等；不等就逐间比对源文件。
- ③ 应输出「校验通过 ✔」。

---

### 📦 提交清单

```bash
git add <精确列出本次改动的文件>          # ⚠️ 不要用 git add -A
git commit -m "feat: x.xx日订单数据更新"
git pull --rebase                        # 若报 cannot pull with rebase: You have unstaged changes
                                         #   → git stash push && git pull --rebase && git stash pop
git push
git log --oneline -1                     # 确认落地
```

- ⚠️ **不要用 `git add -A`**：工作区常年躺着别人的半成品（如 `直播间销量汇总工具.html`、`_vw.py`、`gen_watch_compare.py`），
  一律**只 add 本次真正改动的文件**，并在回报里说明还有哪些没提交。
- 进度表 xlsx 在桌面、且被 `.gitignore` 排除，**不进 git**，不用管。
- commit message 固定格式：`feat: x.xx日订单数据更新` / `feat: x.xx日业绩数据更新`。

---

### 📣 回报格式（跑完一并发给用户，不要中途逐步确认）

> 文件类型 → 录了哪个日期、几条 → 做了哪些归一/取舍（含依据）→ 校验结果 → commit hash

再加一句给用户的**结论**：当天卖得好不好、明天盯什么。

---

### 🛑 只有这几种情况才停下来问

见下方「零点五」的表格。除此之外**默认全部自动做完**，不逐步确认。

---

## 🟢 零点五、自动执行规则（最高优先级，先看这条）

**用户当天丢过来一个日期前缀的 Excel（`x.xx日业绩.xlsx` / `x.xx日订单.xlsx`），就视为授权跑完整条流程，从解析一路做到 `git push`，中途不要再逐步问用户「要不要继续」「可以执行吗」「是否提交」。**

### 执行方式

1. **认文件**：按「一、第一步」判断是订单文件还是业绩文件（先打开看列名，不要只看文件名）。
2. **认流程**：订单 → 走「二」；业绩 → 走「三」；两个文件同时给到 → 两条流程**各自独立**跑，注意别互相污染（订单流程不许碰 `业绩demo.html`，业绩流程不许碰 `sales_analysis/`）。
3. **一路做完**：解析 → 写文件 → 自校验 → 提交推送（按顶部「📦 提交清单」，**不要用 `git add -A`**）。
4. **跑完给结论**：用简表回报「录了什么、有几条、做了什么归一/取舍、校验结果、commit hash」，不要中途每步都停下来等确认。

> 📌 日常照顶部「⚡ 每日执行清单」走即可，本节只补充规则与例外。

### 只有这几种情况才停下来问用户

| 情况 | 处理 |
|------|------|
| 出现从未见过的新直播间名 | 先问归属（见上方 🆕 规则），**不要**静默归到「良米」 |
| 文件既不像订单也不像业绩，或列名对不上 | 问用户这是什么文件 |
| 表里有数值对不上（小时明细加总 ≠ 总 GSV 且差额无法解释） | 说明差异后问用户以哪个为准 |
| 同一天已入库、且新数据与旧数据不一致 | 问用户是覆盖还是忽略 |
| 用户给的日期与文件名日期不符 | 问用户用哪个日期 |
| 主播姓名在页面里查无此人，且无法判断是不是同一人写法 | 问用户确认是不是同一人 |

> 除上表之外，**默认全部自动做完**。姓名同音/错别字等**能由页面既有写法唯一确定**的（如「高姗姗」→「高珊珊」、「 朱泓颖」→「朱泓颖」），直接按既有写法归一，并在回报里说明即可，不用问。
> 自动提交按「五、自动提交规则」执行，commit message 格式固定。

---

## 🚨 第一步：判断文件类型（必须执行）

收到任何 Excel 文件后，**先打开查看内容**，根据列名判断类型：

### 订单文件特征
| 列名 | 说明 |
|------|------|
| 选购商品 | 产品名称 |
| 订单提交时间 | 含 `\t` 前缀，如 `\t2026-08-06 23:59:35` |
| 订单状态 | 已发货 / 已付款 / 已取消等 |
| 订单应付金额 | 数字 |
| 直播间名称 | 如 小米官方手环直播间 |

### 业绩文件特征
| 列 | 说明 |
|----|------|
| A列 | 主播名字 |
| B列 | 主播名字（与A对应） |
| C列 | 场次：A1/A2、B1/B2、C1/C2、D1/D2、E |
| E列 | 每小时数据 |
| F列 | 该主播今天总业绩 |
| G列 | 日期 |

直播间纵向堆叠：第1行=小米数码旗舰店，第27行=小米官方手环直播间，依次往下。

---

## 二、订单文件 → 只更新销售分析

```
判断: 文件名含"订单" 且 列名含 [选购商品, 订单状态, 订单应付金额] + 一个时间列 + 一个直播间列
```

> ⚠️ **列名会变，看含义不看字面**：时间列见过 `订单提交时间` / `支付完成时间`（9.11 起改成后者），
> 直播间列见过 `直播间名称` / `达人昵称`。解析器走关键词匹配，不受影响，但人工认文件时别被字面卡住。

> 🟢 **全自动执行**：收到当日订单文件直接做完并推送，不用逐步问用户——见「零点五、自动执行规则」。

### 步骤

👉 **直接照顶部「⚡ 每日执行清单」的 🅰️ 走**（7 步：认文件 → 入库 → 自校验 → 看事实 → 写稿 → 落库刷新 → 提交推送）。
本节只补充订单流程特有的注意事项。

### ⚠️ 注意事项
- **不要**运行 `update_daily_html.py`（它会修改业绩demo.html）
- **不要**修改 `主播业绩/业绩demo.html`
- 只动 `sales_analysis/` 目录下的文件
- **次日凌晨尾单不去重**：文件在次日凌晨导出，会混入少量次日 00:00~01:00 的订单（实测每天 2~6 笔），下一天的文件里还会有，因此**确实跨天重复计算**。量小，**维持全量入库、不做过滤**（用户 2026-09-14 决定）。不要为此改 `daily_update.py`。

---

## 三、业绩文件 → 只更新业绩面板

```
判断: 文件名含"业绩" 且 Excel格式为 [A=主播, C=场次, F=总业绩, G=日期]
```

> 🟢 **全自动执行**：收到当日业绩文件直接做完并推送，不用逐步问用户——见「零点五、自动执行规则」。

### 步骤

1. 解析 Excel，按直播间逐行提取数据
2. 将每行转为 `{ roomId, shift, anchor, sales }` 格式
3. 插入到 `主播业绩/业绩demo.html` 的 `DAILY_RECORDS` 中（按日期排序），**插在上一天块之后**
4. 自校验：逐条比对 + node 语法校验 + 姓名归一——见「六、入库自校验」
5. 提交并推送

### 解析要点（踩过的坑）

- **总 GSV 在 F 列**（第 6 列），不是 E 列；E 列是分小时明细。取数取 F。
- 偶尔有**总 GSV 掉到错列/小时明细里**的情况（如 9.12 耳机直播间李晓洋 B 班的 3208.6 落在 F 列但小时明细也在 E 列）——用**小时明细加总**验证后再入库。
- 直播间名和「达人」表头行混在数据里，靠**直播间名匹配**切块，块内每遇到一次**非空达人名**就是一条新记录。
- 行尾可能有多余空格（时间、姓名都可能带），全部 strip。

### 直播间 → roomId 映射

| Excel 直播间名 | roomId |
|---------------|--------|
| 小米数码旗舰店 | room_xiaomi_digital |
| 小米官方手环直播间 | room_xiaomi_band |
| 小米官方手表 | room_xiaomi_watch |
| 小米官旗手表直播间 | room_xiaomi_watch_flagship |
| 小米官方耳机直播间 | room_xiaomi_earphone |
| 小米AI眼镜直播间 | room_xiaomi_glasses |
| 小米智能设备旗舰店直播间 | room_xiaomi_smart_device |
| 小米手环官旗直播间 | room_xiaomi_band_flagship |
| 手环预约期业绩（伪直播间，非真实渠道） | room_xiaomi_band_preorder |

### 场次映射
- A1 → shift: 'A', A2 → shift: 'A'
- B1 → shift: 'B', B2 → shift: 'B'
- C1 → shift: 'C', C2 → shift: 'C'
- D1 → shift: 'D', D2 → shift: 'D'
- E → shift: 'E'（E不分上下半场）

### ⚠️ 注意事项
- **不要**运行 `run_all.py sales`（它修改销售分析）
- **不要**修改 `sales_analysis/` 目录下的文件
- 如果出现新的直播间名，同步更新上方映射表

### 🏆 日第一功能模块（业绩demo.html 内）

业绩demo.html 底部包含"日第一"功能，由三个面板组成：

| 面板 | 功能 |
|------|------|
| ⚙️ 日第一有效阈值管理 | 每个直播间独立设置最低销售额门槛，低于阈值不计入有效日第一 |
| 📋 每日各直播间第一 | 按日期展示每个直播间的日冠军、销售额、是否有效 |
| 🏆 累计有效日第一排名 | 统计每位主播累计获得有效日第一的次数，含排名 |

#### 默认阈值

| 直播间 | 阈值 |
|--------|------|
| 小米数码旗舰店 | ¥30,000 |
| 小米官方手环直播间 | ¥30,000 |
| 小米官方手表 | ¥30,000 |
| 小米官旗手表直播间 | ¥15,000 |
| 小米官方耳机直播间 | ¥10,000 |
| 小米AI眼镜直播间 | ¥2,000 |
| 小米智能设备旗舰店直播间 | ¥2,000 |
| 小米手环官旗直播间 | ¥2,000 |

> 阈值可在页面上实时修改，修改后自动应用到下方的日第一排名。

### 🗓️ 主播月度出勤统计模块（业绩demo.html 内）

`主播业绩/业绩demo.html` 中的「🗓️ 主播月度出勤统计」面板，按**月份**汇总每位主播在**每个直播间**的出勤情况：

表格列：`直播间 | 主播 | 出勤天数 | A | B | C | D | E | 月销售额`

| 列 | 含义 |
|----|------|
| 直播间 | 每行都显示直播间徽章 |
| 主播 | — |
| 出勤天数 | 该月有记录的**不同日期数**（同一天多场次只算 1 天） |
| A~E | 该主播在该直播间的**各档班次出勤天数**，只算天数；未出勤显示浅灰 `–` |
| 月销售额 | 该月该主播在该直播间的 GSV 合计（不扣退款） |

- **班次已合并上下半场**：A1/A2 → A，B1/B2 → B，只统计 A/B/C/D/E 五档。
- 列宽：`table-layout: fixed` + **百分比列宽铺满整宽**（20% / 9% / 9% / A~E 各 8% / 22%），每列都分到一份，避免某列超宽导致右侧大片留白；`min-width: 1000px` 保证窄屏时列不被压扁（超出横向滚动）。
- 面板自带**月份**和**直播间**两个下拉框（默认最新月 / 全部直播间），**都独立于顶部筛选栏**，切换不影响其他面板。
- 支持 📥 导出Excel，表头与页面表格同口径；文件名 `主播月度出勤统计_<月份>[_<直播间>].xlsx`，导出内容跟随当前筛选。
- 数据源同 `DAILY_RECORDS`，无需额外维护。

### 📆 顶部「按月」快捷筛选

筛选栏中的 `📆 按月` 是**下拉框**（`#monthFilterSelect`），选项由 `getAvailableMonths()` 动态生成，首项固定为「不限月份」。

> 用下拉框而非按钮组，是因为月份会越来越多，横排按钮会撑爆筛选栏。

- 选中某月 = 把日期区间设为该月**首日 ~ 末日**并全局刷新（等同手动填日期后点「应用」）。
- 选中月份后会清除「近7天/近14天/近30天/全部」按钮的选中态；反之点近N天按钮、点「应用」、或手动改日期框，下拉框都会**复位为「不限月份」**。
- 新增月份数据后无需改代码，下拉选项自动出现。

---

## 三点五、小米手环11 首销月目标进度表（仅 2026-09-07 ~ 10-07）

> ⏳ **有效期**：只在首销月内维护。**2026-10-07 之后本流程停止执行**，脚本 `generate_band11_target.py` 与桌面进度表都不再更新。

我司小米手环11 首销月（9.7 开售 ~ 10.7，共 31 天）四渠道**下单台数目标 60000 台**。
进度表由脚本从 `sales_analysis/history.json` **自动累加生成**，无需手填任何数字。

### 目标拆分依据

按 **9.7 首销日起已入库天数**的累计占比拆分，**含 9.7 开售日**。开售日各渠道吃到的流量是真实结果，承担更多开售红利的渠道相应扛更多目标。

| 渠道 | 9.7-9.11 台数 | 累计占比 | 首销月目标 | 已达成(5天) |
|------|--------------|---------|-----------|------------|
| 小米数码旗舰店 | 13056 | 54.3% | **32600** | 13056 |
| 小米官方手环直播间 | 6623 | 27.5% | **16500** | 6623 |
| 我司商品卡 | 4218 | 17.5% | **10500** | 4218 |
| 小米官方手表 | 146 | 0.6% | **400** | 146 |
| 四渠道小计 | 24043 | 100% | **60000** | 24043 |
| 我司其他直播间 | 290 | — | 不设目标 | 290 |
| **总量** | 24333 | — | **60000** | **24333** |

> **我司其他直播间** = `team_config.OUR_ROOMS` 减去上面四个渠道（当前为 AI眼镜 / 官方耳机 / 官旗手表 / 手环10Pro / 手环官旗 五个直播间），由脚本**自动推导**，以后新增我司直播间会自动纳入。
> 其成交量**计入 60000 台总量、直接冲减剩余，但不单独设目标**，主要分析仍以上面四个渠道为准。

### 步骤

```bash
# 1. 订单文件先按「二、订单文件」入库（run_all.py sales）
# 2. 写当日销售总结（见下方「每日销售总结」）
python band11_review.py status              # 三个数据源最新日期是否对齐
python band11_review.py context 2026-09-13  # 打印写作素材
#    → Claude 据此写 sales_analysis/daily_review_input/<日期>.json
python band11_review.py add 2026-09-13      # 追加进 daily_summary.json

# 3. 刷新首销月进度表（含「每日总结」sheet）
python generate_band11_target.py

# 4. 提交并推送（按顶部「📦 提交清单」，不要用 git add -A）
git add <本次改动的文件> && git commit -m "feat: x.xx日手环11首销月进度更新 + 销售总结"
git pull --rebase && git push
```

### 📝 每日销售总结（每日总结 sheet）

每天在新 sheet「每日总结」里追加**一块**，最新在最上，**历史全部保留**。

每天在新 sheet「每日总结」里追加**一块 4 行**，最新在最上，**历史全部保留**：

```
2026-09-13（周日）   评级：一般
我司全店 3,457 单 / ¥1,156,311（环比 -13.9%）｜ 手环11 2,787 台（环比 -16.7%）｜ 累计 30,464/60,000 = 50.8%
销售总结  ✅ 好：手表间 54 台环比 +170%，是当日唯一上涨的渠道；商品卡 888 台反超数码升到第二
          ⚠️ 差：环比 -16.7% 结束三连涨，手环间少 405 台，占全天 -557 台降幅的 73%
交接要点  🎯 手环间能否回到 1,200 台以上，看工作日流量回归，预计 2-3 天见分晓
```

| 项 | 规则 |
|----|------|
| 写什么 | 固定三小段，**每段一句话**：`✅ 好`（好在哪）/ `⚠️ 差`（差在哪）/ `🎯 盯什么 + 大概多久见分晓` |
| **必须点出好坏** | **不要中性描述**（「整体平稳」「表现一般」这类一律不要）。要具体到**渠道 + 数字 + 对比** |
| **不看主播业绩** | 只看当天整体销售（订单口径）。**不要**出现班次、主播名、主播 GSV —— 那些在 `业绩demo.html` 里，本表不掺和 |
| 长度 | 每段 ≤ 90 字（`band11_review.MAX_FIELD`），超了 `add` 直接拒收 |
| 谁写 | **Claude 每天分析撰写**；没写时脚本按模板兜底并标 ⚪，**任何情况下不留空块** |
| 触发时机 | **必须排在订单入库之后**（第 2 步），只要 `history.json` 有当天数据就能写 |
| 累计 | `daily_summary.json` 只追加、绝不删除历史日期；`add` 对已存在日期默认拒绝，要改只能同一天 `--replace` |
| 有效期 | 与本节同：**2026-10-07 之后停止** |

> 写「🎯 盯什么」时**必须带时间预期**（如「预计 2-3 天见分晓」「1-2 天可确认」），
> 否则接班的人不知道该盯多久、什么时候该回头复盘。

**涉及的文件**

| 文件 | 作用 |
|------|------|
| `band11_review.py` | 算事实（`day_metrics`）+ 存总结（`daily_summary.json`），CLI：`status` / `context` / `auto` / `add` / `validate` |
| `sales_analysis/daily_summary.json` | **总结的唯一持久化主库**，必须 git 提交 |
| `sales_analysis/daily_review_input/<日期>.json` | 人写的稿子，字段只有 `rating` / `good` / `bad` / `watch` 四个，`add` 从这里读 |

稿子长这样（照抄改数字即可）：

```json
{
  "rating": "一般",
  "good": "手表间 54 台环比 +170%，是当日唯一上涨的渠道；商品卡 888 台反超数码升到第二",
  "bad": "环比 -16.7% 结束三连涨，手环间少 405 台，占全天 -557 台降幅的 73%",
  "watch": "手环间能否回到 1,200 台以上，看工作日流量回归，预计 2-3 天见分晓"
}
```

> 为什么总结必须外置成 JSON：`generate_band11_target.py` 的 `main()` 每次都 `Workbook()` 从零重建整个工作簿，**任何只写在 xlsx 里的内容第二次跑就永久消失**。

**评级**（`band11_review.grade()`）：**好 / 一般 / 差**三档，主判据是节奏比 = 当日手环11 台数 ÷ 首销月日均需求（60000/31≈1,935 台）：

| 评级 | 条件 |
|------|------|
| 好 | 节奏比 ≥ 1.00 且环比 ≥ -5% |
| 一般 | 节奏比 ≥ 0.75 且环比 ≥ -30% |
| 差 | 其余 |

首销日无环比基准记「好」；**开售次日跳过环比判据**（9.7 的峰值不该由 9.8 承担）。人工写的 `rating` 覆盖脚本评级。`add` 校验会拒绝超过 4 段的 `summary`。

### 输出

`桌面/小米手环11首销月目标进度表.xlsx`，5 个 sheet：

| Sheet | 内容 |
|-------|------|
| 进度总览 | 总目标 / 已达成 / 累计达成率 / 时间进度 / 进度差 / 剩余台数；并按近 4 天日均推算**预计达成日**。已达成 = 四渠道 + 我司其他直播间 |
| **每日总结** | **每天一块、每块 4 行**（最新在最上）：头行 = 日期/星期/评级，次行 = 当日数字（脚本算），下接「销售总结」2-3 句 +「交接要点」1-2 句。**只看整体销售，不含主播业绩** |
| 分渠道目标 | 各渠道 目标 · 已达成 · 达成率 · 进度差 · 剩余台数 · 剩余日均需求 · 近4天日均 · 缺口富余 · 状态（领先/正常/落后）；底部含「我司其他直播间」冲减行 |
| 每日明细 | 9.7~10.7 **逐日一行**：四渠道 + 我司其他直播间台数 + 当日/累计合计 + 累计达成率 + 时间进度 + 线性目标 + 进度差，含柱线组合图 |
| 拆分说明 | 拆分口径、剔除项、数据口径、有效期 |

### 关键规则

- **历史数据全部保留**：脚本读 `history.json` 全区间的数据，每天只是在表里追加一行，**已入库日期不会被覆盖**，可以看到每天的数据。
- **未到日期留空**：灰底行不参与任何计算，避免把「还没到」误读成「没达成」；趋势图的累计线只画到最后一个已入库日期。
- **口径**：下单台数 = 订单条数，**不扣退款**，与销售分析看板同口径。
- **总达成 = 四渠道 + 我司其他直播间**：其他直播间不设目标，其成交量体现在「剩余台数」上（冲减行显示为负数）。
- **改目标**：`TOTAL_TARGET` 和 `ROOM_TARGETS` 写死在 `generate_band11_target.py` 顶部配置区，要调改那里。
- **我司其他直播间不用手工维护**：由 `team_config.OUR_ROOMS` 自动推导，跟随直播间归属配置走。
- 状态判定：近 4 天日均 ≥ 剩余日均需求 × 1.1 → 领先；≥ 需求 → 正常；否则落后。
- **两个「销售额」口径不能相减**：进度表 / 块头的金额取 `history.json`（订单**实付**），各班次 GSV 取 `业绩demo.html`（主播业绩，**未扣退款**）。9.12 手环间 455,523 vs 472,176 不是 bug。
- **手环11 台数口径**：`load_daily()` 返回的 dict 里既有「我司其他直播间」的**聚合行**、又有被它聚合的**单间**，直接 `sum(values())` 会重复计数。统一用 `band11_review.b11_of()`（= 四渠道 + 我司其他直播间）。

### ⚠️ 注意事项

- 本脚本**只读** `history.json`，不修改任何数据文件；必须跑在订单入库**之后**。
- 不要把它塞进 `run_all.py`——首销月结束就要停，留在外面手动跑更好摘除。
- **不要在桌面 xlsx 里手工编辑**（加备注、调格式、改文字）——`main()` 每次都从零重建整个工作簿，手改会在下次重跑时**无声丢失**。要改总结就改 `sales_analysis/daily_summary.json`。
- 跑之前**先关掉 Excel 里打开的这张表**，否则 `wb.save()` 会抛 `PermissionError`（脚本已把它转成中文提示）。
- 总结 sheet 由「订单入库 → 写总结 → 跑脚本」三步产出，详见上方「📝 每日销售总结」。

---

## 四、月度切换（新月份自动准备页面）

> 换月时（收到新月份第一个订单/业绩文件，或用户提及换月），**无需用户提醒**，按下面流程自动准备下个月页面。

### 触发时机
- 文件名日期前缀跨月（如 `8.31日订单` → `9.1日订单`），或历史最后一天与新文件日期不在同月。
- **先**准备新月份页面，**再**按「二、订单文件」/「三、业绩文件」处理当日数据。

### 步骤

1. **生成新月份总结页脚本 + 占位页**
   - 模板：`generate_september_summary.py`（内含占位页逻辑，无数据时生成占位页）。
   - 复制为 `generate_<下月英文>_summary.py`（10月 → `generate_october_summary.py`），做全局替换：

   | 模板（9月） | 换成（10月 为例） |
   |---|---|
   | `2026-09` | `2026-10`（⚠️ 月末天数：`2026-09-30`→`2026-10-31`） |
   | `9月` / `九月` | `10月` / `十月` |
   | `September` / `september` | `October` / `october` |
   | 周标签 `9/1-9/7` … `9/29-9/30` | `10/1-10/7` …（按当月实际天数） |
   | 改进建议文案 | 季节/节点 + 对比基准月 +1（如「与9月同期」「延续9月势头」「根据9月基准」） |
   | 占位页「返回」链接 | 指向上一个月（如 `八月销量分析.html`） |
   | `.nav-bar` 导航 | 插入上一个月链接（非 active）+ 当前月 active |

   - 运行 `python generate_<下月>_summary.py` → 生成 `月度总结/<X>月销量分析.html`。

2. **更新 `modules.json`**
   - `sales.subModules` 追加：`{ "id": "sales<Month>", "title": "<X月>", "monthKey": "2026-0X", "badge": "NEW" }`，并移除上月的 `badge`。
   - `monthlySummary.subModules` 追加：`{ "id": "summary<Month>", "title": "<X月>", "url": "月度总结/<X>月销量分析.html", "badge": "NEW" }`。

3. **更新各月页面导航**（`月度总结/` 下 6月/7月/8月/9月… HTML 的 `.nav-bar`）
   - 每个页面都加入新月份链接；当前月标 `active`。

4. **提交并推送**
   ```bash
   git add <本次新增/改动的文件> && git commit -m "feat: 新增<X>月销量分析页面及导航" && git push
   ```

### 说明
- 新月份总结页是**占位页**（「X月数据尚未开始」），待当月订单录入后重跑 `generate_<X>_summary.py` 即自动填充真实数据。
- `sales_analysis/index.html` 的日期/月份由 `history.json` 动态生成，无需手动加 tab；`modules.json` 的 `monthKey` 是首页侧边栏的月份入口。

---

## 四点五、设计令牌（theme.css）

根目录 `theme.css` 是全站**唯一的视觉令牌来源**：品牌色、中性阶、语义色、圆角 3 档、阴影 3 档、间距阶、模块强调色。

### 规则

- **新页面必须引入**：`<link rel="stylesheet" href="theme.css">`（子目录页面用 `../theme.css`），**放在该页自己 `<style>` 之前**，这样页面内联规则仍可覆盖。页面不要自己再定义一套 `:root`。
- **`common.css` 不要动**：它还服务 `主播分析/`、`主播管理/`、`竞争方案/`、`运营工具/` 等页面；改它的 `:root` 会波及那些页。
- **数据语义色不要在 theme.css 里覆盖**：月度总结页的 `--clr-ours`（我方蓝）/`--clr-comp`（竞对橙）、违规报告页的 `--accent`（违规红）、业绩页 `ROOMS` 数组里的直播间色——这些在**编码数据含义**，不是装饰色。
- **模块强调色**：在 `theme.css` 里按 `.module-card.<模块id>, .sidebar-nav a.<模块id> { --mod: …; --mod-bg: …; }` 加一行。**没加规则的新模块会自动落到默认小米橙，不会再出现「卡片掉色」**（首页历史上 `scripts`/`exam` 漏写就是这个原因）。

### 换月时自动生效

`generate_september_summary.py`（换月模板）已经带上了 theme.css 链接和统一令牌，所以复制出新月份生成器时**无需额外处理**。各月份页面的 `.nav-bar` 仍需按「四、月度切换」加入新月份链接；历史的 `generate_june/july/august_summary.py` 里的导航也要同步补链接，否则重跑会把手工加的链接冲掉。

### 改动视觉时的红线

改样式前先确认没动到 JS 依赖的选择器——详见各页面的硬约束：

| 页面 | 绝对不能改 |
|------|-----------|
| `index.html` | `#sidebarNav` 的模块链接必须是它的**直接子元素**（JS 用 `>` 选择器）；`#overview` 内 4 张 `.stat-card` 顺序数量不可变（JS 按 `[0]~[3]` 索引）；全局函数名（内联 `onclick` 引用） |
| `主播业绩/业绩demo.html` | 35 个唯一 id 目标（58 处 `getElementById` 调用）、`.filter-btn.active`、`#anchorMonthlyStatsPanel` 的 9 列表头 + `table-layout:fixed` |
| `sales_analysis/index.html` | `.prow` `.rrow` `.collapsed` `.hidden` `.extra-row` `.tab-prodroom` `.roomdet-panel` |
| 月度总结页 | id 由生成器产出，**改样式要改生成器**，只改 HTML 会被下次重跑覆盖 |

> 视觉改完跑一次 `python ui_check.py <标签>` 可自动核对：各页 JS 错误数、JS 引用的选择器是否仍可解析、关键数值是否与基线一致。改之前先跑一次 `python ui_check.py before` 存基线，改完再跑一次对比。

---

## 五、自动提交规则

- **整条流程都是自动的**，不只是提交这一步——详见「零点五、自动执行规则」。
- 具体命令照顶部「📦 提交清单」执行（**明确列出改动文件，不要用 `git add -A`**）。
- **不需要**等用户确认，也不要在解析完、写完文件后停下来问「要不要提交」
- commit message 格式：`feat: x.xx日订单数据更新` 或 `feat: x.xx日业绩数据更新`
- 提交前**先自校验**（见「六、入库自校验」），校验不过就先查原因，不要带着疑问提交
- push 被拒（远端有新提交）→ `git pull --rebase`；若报 `cannot pull with rebase: You have unstaged changes`，
  先把无关改动 `git stash push`，rebase 完再 `git stash pop`

---

## 六、入库自校验（提交前必做）

自动执行的前提是**自己先把错挡住**，所以每次入库后、commit 前跑一遍校验。目的是防住「解析错行 / 数字抄错 / 场次串档 / 主播名字打成新的一行」这几类会污染累计统计的错误。

### 订单文件

- `run_all.py sales` 跑完看输出里的**总条数、日期、各直播间条数**是否与源文件对得上。
- 抽查当天条数最多和最少各一个直播间，与 Excel 里手动筛选的条数比对。
- **必查新记录的 `date` 是不是 `NaT`**：`daily_update.py:195` 是 `str(df['date'].iloc[0])`，取**第一行**的支付时间。文件首行若是一笔「待支付」订单（没有支付完成时间），整条记录的 `date` 就变成 `NaT`——`load_daily()` 会 `strptime` 失败并**静默跳过这一天**（进度表莫名少一天，不报错），且同日 upsert 失效、重跑会追加出第二条。
  ```bash
  python -c "import json;H=json.load(open('sales_analysis/history.json',encoding='utf-8'));print(H[-1]['date'],H[-1]['total_orders'])"
  ```
  查到 `NaT` 就**让用户重新导出**（排除待支付），再按「修坏数据的标准动作」清理（先备份 → 带断言删那一条 → 重跑）。
- 源文件行数对不上时，先减掉 `IGNORED_ROOMS` 里的**小米手环手表直播间**（约 5 单/天），再比对。

### 业绩文件

1. **逐条比对**：脚本重新解析 Excel 并按 `roomId + shift + anchor + sales` 排序，与页面上 `DAILY_RECORDS['<当天>']` 解析出来的对象数组排序后比对，必须**完全一致**（含条数）。
2. **语法校验**：把 `DAILY_RECORDS` 整块抽出来交给 node 解析一遍，确认 43 个日期块没被破坏、最后一天是当天日期。
3. **主播姓名归一**：
   - Excel 里的**前后空格**（如 `' 朱泓颖'`）必须 strip。
   - 姓名与页面既有写法不一致时（如「高姗姗」vs「高珊珊」），**按页面既有写法归一**，不要新建一个人。
   - 归一后要确认该名字在页面里**不是孤例**。

### 对不上怎么办

- **不要**为了跑通而放宽校验或手改数字。
- 差额如果能由明细解释（例如总 GSV 掉到错列、某小时为空），在回报里写清楚依据再入库。
- 解释不了就按「零点五」的表停下来问用户。

### 🚑 修坏数据的标准动作

改动 `history.json` 这类**累计主库**（唯一副本）时：

1. **先备份**：`cp sales_analysis/history.json "sales_analysis/history.json.bak-$(date +%Y%m%d-%H%M)"`
   （`.gitignore` 已忽略 `*.bak-*`，不会污染仓库）
2. **带严格断言再落盘**：脚本里先 `assert` 总条数、目标记录的特征值（日期/条数）、前后邻居、
   其余记录全部合法，**全过才写**。自动模式会拦截「未点名的批量删除」，带断言的最小改动才过得去。
3. **清掉坏运行的附带产物 —— 兄弟文件也要查，不只主文件**：
   坏运行不止写 `history.json`，还会往 `band11_history.json`、`b10pro_history.json`、`hourly/` 里写。
   只清主文件会留下「静默显示错数据」的尾巴。
   ```bash
   # 一次查完所有数据文件的 NaT 残留
   python -c "import json,glob;[print(f,[r.get('date') for r in json.load(open(f,encoding='utf-8')) if r.get('date')=='NaT']) for f in glob.glob('sales_analysis/*_history.json')]"
   ls sales_analysis/hourly/ | grep -v '^2026'   # 有非日期文件名 = 垃圾
   ```
   > 📅 2026-09-13 的 NaT 事故就漏清过 `band11_history.json` 一条，直到 09-14 才被发现
   > ——它排在数组末尾，正好命中页面 `[length-1]` 的兜底，会**显示错数据**。详见 `docs/06-踩坑与故障.md` 第 2 条。
4. 修完重跑一遍入库 + 自校验，确认与源文件对得上。

> 2026-09-13 实战：`9.13日订单.xlsx` 首行是「待支付」（无支付完成时间），
> `daily_update.py:195` 的 `str(df['date'].iloc[0])` 让整条记录 `date` 变成 `NaT`，
> 进度表静默少一天。用户重新导出后，按上面 4 步清理干净。

### 回报格式（跑完一并发给用户）

> 文件类型 → 录了哪个日期、几条 → 做了哪些归一/取舍（含依据）→ 校验结果 → commit hash

---

## 七、变更日志

| 日期 | 变更内容 |
|------|---------|
| 2026-08-07 | 初始创建。明确订单/业绩两条独立流程，加入"先查看再执行"规则 |
| 2026-08-11 | 新增日第一功能模块文档（阈值管理、每日第一、累计排名） |
| 2026-09-02 | 新增规则：遇到新直播间名先找用户确认归属，勿静默兜底归良米 |
| 2026-09-02 | 新增「四、月度切换」标准流程：换月自动准备下月页面，无需用户提醒 |
| 2026-09-11 | 业绩demo.html 新增「主播月度出勤统计」面板 + 顶部「按月」下拉筛选 |
| 2026-09-11 | 出勤统计表格改为 A~E 逐列计数，并固定列宽收紧左侧留白 |
| 2026-09-11 | 出勤统计面板新增直播间下拉筛选；列宽改百分比铺满，消除右侧留白 |
| 2026-09-11 | 从导航移除并删除 4 个停用模块：千川视频分析、小时数据分析、视觉分析、生态链+穿戴（仅改 `modules.json`，导航为动态渲染） |
| 2026-09-11 | 修 `run_all.py` 漏拷 `stats_data.js`；补八月页面「九月」链接；重新生成九月月度总结页 |
| 2026-09-11 | 新增 `theme.css` 统一设计令牌（品牌色/中性阶/语义色/圆角/阴影/间距/模块强调色）；8 个页面接入；修复首页话术库·考试模块卡片掉色、边框过深、移动端侧边栏文字溢出 |
| 2026-09-12 | 业绩demo.html 比例收紧：container 1920→1560px、概览卡封顶 260px 左对齐、各表格按信息量分配列宽（累计总榜徽章不再折行）；index.html 侧边栏 240→268px、基准字号 17.5px 等比放大 |
| 2026-09-12 | 新增「三点五、小米手环11 首销月目标进度表」：60000 台目标按 9.8-9.11 稳态占比拆分到四渠道（22400/21200/15700/700），新增 `generate_band11_target.py` 从 history.json 累加生成桌面 Excel 进度表，仅 9.7-10.7 有效 |
| 2026-09-12 | 首销月进度表纳入「我司其他直播间」：由 `team_config.OUR_ROOMS` 自动推导其余我司直播间，成交量计入 60000 台总量并冲减剩余，但不单独设目标 |
| 2026-09-12 | 首销月目标拆分口径改为**含 9.7 首销日**（9.7-9.11 五天累计占比），目标调整为 32600/16500/10500/400 |
| 2026-09-13 | 新增「零点五、自动执行规则」：当日业绩/订单文件直接跑完整流程到 push，不逐步确认；仅 6 类异常情况才停下来问用户 |
| 2026-09-13 | 新增「六、入库自校验」：订单核条数、业绩逐条比对 + node 语法校验 + 主播姓名归一，原「变更日志」顺延为「七」 |
| 2026-09-14 | 订单尾单口径定案：**不去重、全量入库**（用户决定，量小忽略）；「六、入库自校验」新增「必查 `date` 是否 NaT」——实盘遇到首行为「待支付」导致整条记录日期变 `NaT`、进度表静默少一天，靠重导源文件解决 |
| 2026-09-14 | 首销月进度表新增第 2 个 sheet「每日总结」：每天一块（评级 + 当日数字 + 销售总结 + 交接要点），最新在最上、历史全保留。新增 `band11_review.py`（事实汇总 + CLI）、`sales_analysis/daily_summary.json`（总结持久化主库，**必须 git 提交**）。补录 9.7~9.12 六天总结。 |
| 2026-09-14 | 「每日总结」**大幅简化**（用户反馈「太多了没啥用」）：去掉主播业绩/班次维度（连带删除 `perf_records.py`），每天从 11~13 行压到 **4 行**；评级从 S/A/B/C 改为**好 / 一般 / 差**三档。 |
| 2026-09-14 | 「每日总结」内容改为**结构化模版**（用户反馈「不要都是中性了」）：销售总结固定 `✅ 好` / `⚠️ 差` 两行，交接要点固定 `🎯 盯什么 + 大概多久见分晓`；每段 ≤ 90 字，超长 `add` 拒收。JSON 字段从 `summary`/`handover` 改为 `good`/`bad`/`watch`。 |
| 2026-09-14 | **顶部新增「⚡ 每日执行清单」**：把散在「零点五/二/三点五/五/六」五处的日常步骤收成一份可直接复制的清单（🅰️ 订单 7 步 / 🅱️ 业绩 4 步 / 🅲 两个都来），含「自校验三条铁律」「📦 提交清单」「📣 回报格式」。各详细章节改为只补充规则与例外，避免重复漂移。 |
| 2026-09-14 | 提交规则修正：**明确禁止 `git add -A`**（工作区常年有别人的半成品会被误扫），改为明确列出改动文件；补充 push 被拒时的 `stash + pull --rebase + stash pop` 处理。新增「🚑 修坏数据的标准动作」（先备份 → 带断言落盘 → 清附带产物）。 |
| 2026-09-14 | **新增知识库 `docs/` + 根目录 `CLAUDE.md`**：把散落的字段定义/页面结构/脚本清单/历史坑收成 7 篇文档。直播间归属改为**单向生成**——`team_config.py` → `tools/gen_room_docs.py` → `docs/03` + `直播间分类.md` + `直播间服务商汇总.md`，消灭「两边同步」漂移（该漂移已实际发生过：`直播间服务商汇总.md` 曾漏了「小米耳机」）。新增 `.github/workflows/docs-check.yml` 自动拦截。 |
| 2026-09-14 | 顺带订正两处过期事实：业绩页 `getElementById` 实为 **35 个唯一 id / 58 处调用**（原写 44）；直播间→roomId 映射表补 `room_xiaomi_band_preorder`（手环预约期业绩）。 |
| 2026-09-14 | 修复 `band11_history.json` 里 9/13 事故遗漏的 1 条 `date:"NaT"` 垃圾记录。「修坏数据的标准动作」第 3 步补充**兄弟文件排查**（该漏清会让主打手环模块在日期查不到时显示错数据）。 |
