# CLAUDE.md

小米手环直播间销量分析 —— 直播间销量/业绩数据的日常入库、月度汇总与可视化平台。

## 👉 第一件事：判断用户给了什么

| 用户给了什么 | 怎么做 |
|-------------|--------|
| **一个 `x.xx日订单.xlsx` 或 `x.xx日业绩.xlsx`** | **直接读 `WORKFLOW.md` 顶部的「⚡ 每日执行清单」，照做，一路跑到 `git push`**。这是已授权的全自动流程，**不要逐步确认** |
| 其它问题 | 先查 `docs/`，见下方导航 |

> ⚠️ 日常**只读 `WORKFLOW.md` 顶部那一节**就够了。出问题或命中例外，才往下翻对应章节。

## 📚 知识库导航

详细内容在 `docs/`，从 [`docs/00-索引.md`](docs/00-索引.md) 进：

| 文档 | 内容 |
|------|------|
| [`00-索引`](docs/00-索引.md) | 「我要做什么 → 去哪找」路由表 |
| [`01-数据流`](docs/01-数据流.md) | 三条链路、谁是主库、谁生成谁 |
| [`02-数据字典`](docs/02-数据字典.md) | 各 JSON/JS 字段含义（**权威**，`data_schema.md` 已过期） |
| [`03-直播间与团队`](docs/03-直播间与团队.md) | 归属总表 + roomId（⚙️ 自动生成） |
| [`04-页面与模块`](docs/04-页面与模块.md) | 页面清单（手写 vs 脚本生成）+ **改样式红线** |
| [`05-脚本清单`](docs/05-脚本清单.md) | 30 个脚本：活跃 / 按需 / 废弃 |
| [`06-踩坑与故障`](docs/06-踩坑与故障.md) | 真踩过的坑 + 修法 |

## 🚨 三条铁律

1. **两条流程不许互相污染**：订单流程只碰 `sales_analysis/`；业绩流程只碰 `主播业绩/业绩demo.html`。
2. **禁止 `git add -A`**：工作区常年躺着别人的半成品（`直播间销量汇总工具.html`）。
   一律 `git add <本次真正改动的文件>`。

## 📁 目录约定

| 位置 | 放什么 |
|------|--------|
| 根目录 | 日常入口（`run_all.py` / `band11_review.py` / `generate_band11_target.py`）+ 共享库 + 页面资源 |
| `tools/` | 活跃但非日常：月报生成器、周报、`migrate_reclassify.py`、`gen_room_docs.py` |
| `archive/` | 已结项，**别跑**（见 `archive/README.md`） |
| `_artifacts/` | 可再生产物（`ui_check.py` 截图、历史 xlsx）。可随时删 |
| 页面目录 / `sales_analysis/` | ⚠️ **不能移动、不能改名**（公开 URL + 硬编码相对路径） |
3. **脚本生成的文件，改 HTML 无效**：改生成器 `.py`，否则重跑覆盖（清单见 `docs/04`）。

## ⚙️ 改过 `team_config.py` 就要重跑生成器

`直播间归属` 是单向生成的，**不要手改 markdown**：

```bash
python tools/gen_room_docs.py           # 重新生成 docs/03 + 直播间分类.md + 直播间服务商汇总.md
python tools/gen_room_docs.py --check   # 只检查，退出码 1 = 过期（CI 会跑）
```

> `直播间服务商汇总.md` 是 `直播间销量汇总.py` 的**运行时输入**，有硬格式契约。
> 生成器会调真正的解析器回读断言，解析不过就不落盘。

## 🆕 遇到新直播间

**先问用户确认归属**，不要静默兜底归「良米」（`classify_room()` 的兜底是静默的）。

## 🛑 只有这几种情况才停下来问

新直播间名 / 文件类型认不出 / 数值对不上且解释不了 / 同日重复入库且数据不一致 /
日期与文件名不符 / 主播姓名查无此人。除此之外**默认全部自动做完**。

完整表格见 `WORKFLOW.md`「零点五」。

## 📦 提交

```bash
git add <精确列出本次改动的文件>
git commit -m "feat: x.xx日订单数据更新"   # 或 "feat: x.xx日业绩数据更新"
git pull --rebase && git push
git log --oneline -1
```

push 被拒：`git stash push && git pull --rebase && git stash pop`。
