# 05 · 脚本清单

> 被跟踪的脚本（Python + 数据小管家相关的 JS / Worker）按**位置**分类。跑之前先看这张表。

---

## 📁 目录约定（2026-09-14 整理后）

| 位置 | 放什么 | 谁在用 |
|------|--------|--------|
| **根目录** | 日常入口 + 共享库 + 页面必需资源 | 你每天敲的命令就在这层 |
| **`tools/`** | 活跃但**非日常**：月报、周报、迁移、归属文档生成、悬浮组件注入 | 换月 / 做周报 / 改归属 / 改小管家时才跑 |
| **`archive/`** | **已结项 / 已取代**，不参与任何流程 | 只在要重出旧报告时翻出来 |
| **`sales_analysis/`** | 数据链路的两个脚本（与页面/数据同目录） | `run_all.py` 调它 |
| **`_artifacts/`** | **可再生的中间产物**（`ui_check.py` 的截图快照、历史 xlsx 输出） | 不是代码，可随时删 |
| **`assistant-server/`** | 数据小管家的后端（Cloudflare Worker）+ 3 个端到端测试 | 只在改小管家时进 |
| **`cloudbase-assistant/`** | 小管家的腾讯云备用版（**未部署**） | 一般不用管 |

> ⚠️ **不要用 `git add -A`**：`直播间销量汇总工具.html` 是别人的半成品（`_vw.py`、`gen_watch_compare.py` 已于 2026-09-26 清理移除）。
> `_artifacts/` 与 `archive/` 同理——按文件名精确 add。

---

## 🟢 根目录 · 日常入口（每天用到）

| 文件 | 职责 | 入口 | 输入 → 输出 |
|------|------|------|------------|
| `run_all.py` | **订单链路总入口** | `python run_all.py sales <excel> [our_excel]` | Excel → 调 `daily_update.py`，再把产物拷到 `output/` |
| `band11_review.py` | 首销月「算事实 + 存总结」 | `python band11_review.py status\|context\|auto\|add\|validate [日期]` | `history.json` → `daily_summary.json` |
| `generate_band11_target.py` | 生成首销月进度表 | `python generate_band11_target.py` | `history.json` + `daily_summary.json` → 桌面 xlsx |
| `ui_check.py` | UI 改造前后一致性校验 | `python ui_check.py <标签>` | 7 个页面 → `_artifacts/_ui_<标签>_*.png/json` |

> ⚠️ `band11_review.py` 与 `generate_band11_target.py` **互为 import**（后者在函数内反向 import 前者以破循环），
> **必须同目录**。

## 🟢 根目录 · 共享库（被多处 import）

| 文件 | 职责 |
|------|------|
| `team_config.py` | **直播间归属唯一配置源**（`TEAM_MAP` / `OUR_ROOMS` / `LIANGMI_ROOMS` / `IGNORED_ROOMS`） |
| `product_classifier.py` | 商品名归一唯一源 |
| `utils.py` | `to_num`、`detect_excel_columns` |
| `formatters.py` | 数字/金额/百分比格式化 |

> `formatters.py` 现在只被 `archive/` 里的脚本引用，但**保留在根目录**——它是通用库，
> 归档脚本已通过 `sys.path.insert(ROOT)` 仍能 import 到它。

## 🟢 根目录 · 独立工具

| 文件 | 职责 |
|------|------|
| `直播间销量汇总.py` | 订单 xlsx → 按服务商汇总。⚠️ 读**同目录**的 `直播间服务商汇总.md`，**两者不能分居** |
| `ui_check.py` | 见上 |

---

## 🟡 `tools/` · 活跃但非日常

| 文件 | 职责 | 入口 |
|------|------|------|
| `gen_room_docs.py` | **归属文档生成器**：`team_config.py` → `docs/03` + `直播间分类.md` + `直播间服务商汇总.md` | `python tools/gen_room_docs.py [--check]` |
| `inject_widget.py` | **悬浮小管家注入器**：把 `assistant-widget.js` 注入各页 `</body>` 前一行（幂等） | `python tools/inject_widget.py [--check\|--remove]` |
| `extract_anchor_records.py` | 业绩数据抽取：`业绩demo.html` → `主播业绩/anchor_records.json`（小管家读它） | 业绩入库后跑（见 `交接说明.md` 4.5 步） |
| `generate_september_summary.py` | 9月月报，**兼换月模板** | `python tools/generate_september_summary.py` |
| `generate_june_summary.py` / `july` / `august` | 各月月报 | `python tools/generate_<月份>_summary.py` |
| `gen_weekly_report.py` | 周报 xlsx（数值硬编码） | `python tools/gen_weekly_report.py` |
| `migrate_reclassify.py` | 改过 `team_config` 后**回溯重算**历史归属 | `python tools/migrate_reclassify.py [--apply]`，⚠️ 会改写 `history.json` |
| `generate_headphone_report.py` | 耳机专报页 | `python tools/generate_headphone_report.py` |

> ⚠️ **这些脚本都住在 `tools/`，项目根是上一级**。它们的顶部统一是：
> ```python
> ROOT = Path(__file__).resolve().parent.parent
> sys.path.insert(0, str(ROOT))     # ← 少这一行就会 ModuleNotFoundError
> DATA_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
> ```
> 其中 `gen_weekly_report.py`、`generate_august_summary.py`、`generate_september_summary.py`
> **原先没有 `sys.path.insert`**（靠「脚本目录 = `sys.path[0]`」侥幸工作），搬进 `tools/` 时已补上。
> **复制新月份脚本时必须保留这段**，否则报错会指向 `import team_config`，误导排查方向。

---

## 🔴 `archive/` · 已结项（别跑）

完整说明见 **`archive/README.md`**。速览：

| 文件 | 为什么归档 |
|------|-----------|
| `update_daily_html.py` | **`WORKFLOW.md` 明令「不要运行」**，会整块改写 `业绩demo.html` 的 `DAILY_RECORDS` |
| `build_html.py` | 618 页面生成器；**import 即执行**（无 `__main__` 守卫） |
| `generate_618_data.py` / `_v2.py` | 618 已结项，v1 被 v2 取代 |
| `generate_report_docx.py` | 输出文件不存在，无引用 |
| `generate_band_improvement_docx.py` | 输出文件不存在，无引用 |
| `generate_anchor_report.py` | 依赖的 `anchor_summary.json` 已不在桌面，**重跑直接崩** |
| `generate_qianchuan_w5.py` | 千川 W5 一次性，输出目录不存在 |
| `update_product_categories.py` | 2026-06-26 一次性；⚠️ 会改写 `history.json` |
| `618_analysis_data.json` | 随 618 脚本归档的数据 |

---

## 📦 `sales_analysis/` · 数据链路

| 文件 | 职责 | 备注 |
|------|------|------|
| `daily_update.py` | **订单入库主脚本**（只有这一份，根目录没有） | `ROOT = parent.parent`，且 `from sales_analysis.build_html import` —— **所以 `sales_analysis/` 不能改名、不能移层** |
| `generate_dashboard.py` | 画 3 张 PNG | 被 `daily_update.py` 函数级 import，**无 `__main__` 守卫** |
| `build_html.py` | **空壳**：只检查 `index.html` 是否存在并 print，不做事 | 别和 `archive/build_html.py`（618 生成器，78KB）搞混 |

---

## 🧩 `assistant-server/` + 前端组件 · 数据小管家（2026-09-26 上线）

不是 Python，但属于这个项目，一并登记。**完整维护手册见 `交接说明.md` 第十二节。**

| 文件 | 类型 | 职责 | 什么时候跑 |
|------|------|------|-----------|
| `assistant-widget.js`（根目录） | 前端 | **右下角悬浮小球本体**：Shadow DOM 隔离、零依赖 | 改外观/交互时改它，改完 **`VERSION`+1** |
| `数据小管家/index.html`（模块页） | 前端 | 全屏聊天页，和悬浮球读同一后端 | 改全屏版界面时 |
| `assistant-server/worker.js` | Worker | 后端：提示词、口径规则、6 个查询工具 | 回答不准 / 加新能力 |
| `assistant-server/wrangler.toml` | 配置 | 域名绑定（`routes`）+ KV 命名空间 | 换域名时 |
| `assistant-server/test-widget.cjs` | 测试 | **悬浮组件端到端**（迷你 DOM 桩跑脚本，30 项断言） | 改完组件**必跑** |
| `assistant-server/test-page.cjs` | 测试 | 模块页端到端（10 项） | 改完模块页跑 |
| `assistant-server/test-accuracy.cjs` | 测试 | **对抗性答题准确性**（6 个真实问题 + 本地独立算期望值对账） | 改完提示词/工具跑 |
| `assistant-server/.dev.vars` | 密钥 | 本地 DeepSeek API Key。**已 gitignore，绝不入库** | — |
| `cloudbase-assistant/`（整目录） | 备用 | 腾讯云 CloudBase 版，功能等价、**未部署** | 备用，一般不动 |

```bash
# 三个测试（都在 assistant-server/ 下跑）
node test-widget.cjs      # 悬浮组件 30 项，含真实问答
node test-page.cjs        # 模块页 10 项
node test-accuracy.cjs    # 答题准确性 6 题

# 改完后端要部署才生效
cd assistant-server && npx wrangler deploy
```

> ⚠️ `worker.js` 的提示词是 JS **模板字符串**，里面不能出现反引号 —— 一旦有，整个 Worker 语法错误、接口全线 500。

---

## 🚧 他人半成品（不要当项目代码）

| 文件 | 是什么 |
|------|--------|
| `直播间销量汇总工具.html` | 拖 Excel 出「直播间销量战报」的独立工具 |

> 2026-09-26 清理：已删除 `_vw.py`（Playwright 调试脚本）、`gen_watch_compare.py`（手表对比 xlsx 生成），
> 二者均为一次性调试产物，未被任何流程或模块引用。

---

## 依赖关系速记

```
run_all.py ──▶ sales_analysis/daily_update.py ──▶ generate_dashboard.py
                        │
                        └──▶ history.json ──┬──▶ tools/generate_{june,july,august,september}_summary.py
                                            ├──▶ band11_review.py ──▶ daily_summary.json
                                            │                              │
                                            └──────────────────────────────┴──▶ generate_band11_target.py

team_config.py ──▶ tools/gen_room_docs.py ──▶ docs/03 + 直播间分类.md + 直播间服务商汇总.md
                                            └──▶ tools/migrate_reclassify.py（改归属后重算历史）
```
