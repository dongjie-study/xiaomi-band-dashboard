# 00 · 索引（不知道去哪找就看这里）

> 小米手环直播间销量分析 — 知识库入口
> **执行层**在 `WORKFLOW.md`，**知识层**在 `docs/`。日常跑数据只需要前者。

---

## 一、按「我要做什么」找

| 我要做什么 | 去哪 |
|-----------|------|
| **今天要录订单 / 业绩** | `WORKFLOW.md` 顶部「⚡ 每日执行清单」——**日常只看这一节** |
| 收到一个 Excel，不知道是订单还是业绩 | `WORKFLOW.md`「🚨 第一步：判断文件类型」 |
| 某个直播间属于哪个团队？ | `直播间分类.md`（速查）或 [`03-直播间与团队.md`](03-直播间与团队.md)（全量对照 + roomId） |
| **新增了一个直播间** | [`03-直播间与团队.md`](03-直播间与团队.md) §八 |
| 某个 JSON / 字段是什么意思 | [`02-数据字典.md`](02-数据字典.md) |
| 数据从哪来到哪去、谁生成谁 | [`01-数据流.md`](01-数据流.md) |
| 要改某个页面的样式 / 内容 | [`04-页面与模块.md`](04-页面与模块.md) —— **先看「红线」** |
| 某个 `.py` 是干嘛的、还能不能跑 | [`05-脚本清单.md`](05-脚本清单.md) |
| 报错了 / 数字对不上 | [`06-踩坑与故障.md`](06-踩坑与故障.md) |
| 换月了（新月份第一个文件） | `WORKFLOW.md`「四、月度切换」 |
| 手环11 首销月进度表 | `WORKFLOW.md`「三点五」（**仅 2026-09-07~10-07 有效**） |
| 改视觉令牌 / 主题色 | `theme.css` + `WORKFLOW.md`「四点五、设计令牌」 |

---

## 二、按「这是什么」找

| 名称 | 是什么 | 唯一主库？ |
|------|--------|-----------|
| `sales_analysis/history.json` | **销售累计主库**，一天一条，105 天（06-01 起） | ⭐ 唯一副本 |
| `sales_analysis/daily_summary.json` | **每日销售总结主库**（评级/好/差/盯什么） | ⭐ 唯一副本 |
| `主播业绩/业绩demo.html` 的 `DAILY_RECORDS` | **主播业绩主库**（2026-08 起） | ⭐ 唯一副本 |
| `team_config.py` | **直播间归属唯一配置源** | ⭐ |
| `product_classifier.py` | 商品名归一唯一源 | ⭐ |
| `modules.json` | 首页模块与导航配置（`index.html` 运行时 fetch） | ⭐ |

> ⚠️ 三个「主库」都**只有一份、且都在 git 里**。改它们按
> `WORKFLOW.md`「🚑 修坏数据的标准动作」走：**先备份 → 带断言落盘**。

---

## 三、这个项目的三条铁律

1. **两条流程不许互相污染**
   订单流程只碰 `sales_analysis/`；业绩流程只碰 `主播业绩/业绩demo.html`。
2. **`git add` 要列文件名，禁止 `git add -A`**
   工作区常年躺着别人的半成品（`直播间销量汇总工具.html`、`_vw.py`、`gen_watch_compare.py`）。
3. **脚本生成的文件，改 HTML 无效**
   改样式必须改生成器 `.py`，否则下次重跑覆盖（清单见 [`04-页面与模块.md`](04-页面与模块.md)）。

---

## 四、维护这个知识库

```bash
python tools/gen_room_docs.py           # 归属文档重新生成（改过 team_config.py 后必跑）
python tools/gen_room_docs.py --check   # 只检查是否过期，退出码 1 = 过期
```

- `docs/03` 和 `直播间分类.md`、`直播间服务商汇总.md` 里的机械表格**都是自动生成的**，
  手改会在下次重跑时丢失。要改归属 → 改 `team_config.py` → 重跑脚本。
- `docs/` 下其余文件（01/02/04/05/06）是**手写维护**的，直接改。
